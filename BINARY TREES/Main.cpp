#include <iostream>

#include "Conjunto.h"

using namespace std;

int main() {

    Conjunto arbol;

    arbol.insertar(12);
    arbol.insertar(5);
    arbol.insertar(89);
    arbol.insertar(48);
    arbol.insertar(37);
    arbol.insertar(22);
    arbol.insertar(59);
    arbol.insertar(99);
    arbol.insertar(8);
    arbol.insertar(3);

}